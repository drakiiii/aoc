# =============================================================================
# GLOBAL IGNORE RULES
# =============================================================================
# This file ensures that no sensitive or machine-specific files are committed.
# Update as needed when new tooling or secrets are introduced.
# =============================================================================

# Virtual environments (if created locally)
venv/
.env/